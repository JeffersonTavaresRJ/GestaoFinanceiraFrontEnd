import { DateFormatToStringPipe } from './date-format-to-string.pipe';

describe('DateFormatToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
